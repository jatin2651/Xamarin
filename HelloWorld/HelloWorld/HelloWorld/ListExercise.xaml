<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HelloWorld.ListExercise">

    <StackLayout>
    <SearchBar Placeholder="Searh..." TextChanged="SearchBar_TextChanged"/>
    <ListView x:Name="listView" IsPullToRefreshEnabled="True" Refreshing="listView_Refreshing" ItemSelected="listView_ItemSelected" >
            <ListView.ItemTemplate>
                <DataTemplate>

                    <TextCell Text="{Binding Location}" Detail="{Binding CheckIn}"  >
                        <TextCell.ContextActions>
                            <MenuItem Text="Call" Clicked="Call_Clicked" CommandParameter="{Binding .}"/>
                            <MenuItem Text="Delete" Clicked="Deleted_Clicked"  CommandParameter="{Binding .}"/>
                        </TextCell.ContextActions>
                    </TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        
        </ListView>
    </StackLayout>

</ContentPage>